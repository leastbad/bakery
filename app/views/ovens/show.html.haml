%h3= @oven.name

.cookie-info
Cookie in oven:
- if @oven.cookie
    #{pluralize(@oven.cookie.quantity, "cookie")} with #{@oven.cookie.fillings.length == 0 ? "no fillngs" : @oven.cookie.fillings }
    = render partial: 'ovens/cookie_status', locals: { oven: @oven }
- else
    None

%br

= link_to "Prepare Cookie", new_oven_cookies_path(@oven), class: 'button'