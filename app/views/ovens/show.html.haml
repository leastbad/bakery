%h3= @oven.name

.cookie-info
  Cookie in oven:
  - if @oven.cookie
    1 cookie with #{@oven.cookie.fillings.length == 0 ? "no fillngs" : @oven.cookie.fillings }
    = render 'cookie_status', :oven => @oven
  - else
    None

%br

= link_to "Prepare Cookie", new_oven_cookies_path(@oven), class: 'button'
